apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  type: {{ .Values.app.serviceType }}
  ports:
    - port: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Chart.Name }}
